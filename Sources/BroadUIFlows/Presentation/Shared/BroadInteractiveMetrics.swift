import CoreGraphics

enum BroadInteractiveMetrics {
    /// Apple platform minimum for an interactive hit target. This value is
    /// deliberately expressed in unscaled points.
    static let minimumHitDimension: CGFloat = 44
}
